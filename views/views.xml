<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="bote.list">
      <field name="name">bote list</field>
      <field name="model">bote.bote</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="bote.action_window">
      <field name="name">bote window</field>
      <field name="res_model">bote.bote</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="bote.action_server">
      <field name="name">bote server</field>
      <field name="model_id" ref="model_bote_bote"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="bote" id="bote.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="bote.menu_1" parent="bote.menu_root"/>
    <menuitem name="Menu 2" id="bote.menu_2" parent="bote.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="bote.menu_1_list" parent="bote.menu_1"
              action="bote.action_window"/>
    <menuitem name="Server to list" id="bote" parent="bote.menu_2"
              action="bote.action_server"/>
    -->
  </data>
</odoo>